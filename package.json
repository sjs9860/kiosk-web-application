{
  "name": "flask-preact-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:preact && npm run copy:static",
    "build:preact": "cd preact-frontend && npm run build",
    "copy:static": "cp -r preact-frontend/build/* static/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "preact": "^10.22.0",
    "preact-cli": "^3.5.1"
  }
}
