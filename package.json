{
  "name": "flask-preact-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:react && npm run copy:react:public",
    "build:preact": "cd preact-frontend && npm run build",
    "build:react": "cd react-frontend && npm run build",
    "copy:preact:public": "cp -r preact-frontend/build/* public/",
    "copy:react:public": "cp -r react-frontend/build/* public/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "preact": "^10.22.0",
    "preact-cli": "^3.5.1"
  }
}
